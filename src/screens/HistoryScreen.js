import React from "react";

const HistoryScreen = () => {
  return (
    <div>
      {/* <ul>
        {history.map((el, i) => (
          <ul key={i}>
            {el.hour} : {el.min} : {el.sec} : {el.milSec}
          </ul>
        ))}
      </ul> */}
      History
    </div>
  );
};

export default HistoryScreen;
